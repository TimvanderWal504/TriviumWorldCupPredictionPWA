{
  "version": "2026-06-12",
  "notes": "Goal scorers, cards, and substitutions are now fetched reliably after every match.\n\nPreviously, a single network hiccup during result ingestion could silently prevent match events from ever being saved. Completed matches are now retried until their events are confirmed — so if a match finished without any goal scorers showing up, it will back-fill automatically on the next poll.",
  "reminder": "Already played matches may now show goal scorers and cards that were missing before."
}
